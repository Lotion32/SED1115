from machine import Pin
import time

led = Pin(16, Pin.OUT)
button = Pin(12, Pin.IN, Pin.PULL_DOWN)


while True:

    if button.value() == 1 :
        led.value(1)

    else:
        led.value(0)
    
    
